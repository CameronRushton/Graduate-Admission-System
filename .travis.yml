language: java
jdk: openjdk8
os: linux
deploy:
  - provider: heroku
    api_key:
      secure: $HEROKU_API_KEY
    app: "graduate-admission-system"
    on:
      branch: crushton/single-app-deployment